import { model } from 'mongoose';
export function typedModel(name, schema, collection, skipInit, statics) {
    if (schema && statics)
        schema.statics = statics;
    return model(name, schema, collection, skipInit);
}
